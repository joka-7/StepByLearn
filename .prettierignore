dist/
node_modules/
package-lock.json
legacy-python/

# Vendored ogen-ai config and everything ai-sync generates from it. Prettier
# does not own these: .ai/ is upstream code this repo cannot fix from here, and
# the rule files plus docs/STRUCTURE.md are regenerated by `ai-sync` and
# `gen_tree.py` — reformatting them only makes the next regeneration a diff.
.ai/
.agents/
.claude/
.cursor/
.github/skills/
AGENTS.md
CLAUDE.md
GEMINI.md
.github/copilot-instructions.md
docs/STRUCTURE.md
# README.md carries a generated tree region too. Prettier adds blank lines
# inside the BEGIN/END markers, which the next gen_tree run would undo --
# a permanent format-check/docs-drift standoff.
README.md
